const num1 = 5;
const num2 = 10;

function addValues(){
  console.log(`the sum is ${num1 + num2}`)
}

addValues()

// you can import with require(./) directly and this will invokve the module as a function 